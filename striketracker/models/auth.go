package models

/*
// Mode indicates which authentication method was configured
type Mode int

// AuthModes available
const (
	USER Mode = iota
	TOKEN
)

// AuthWrapper test
type AuthWrapper struct {
	Mode           Mode
	Authentication *Authentication
	Authorization  *Authorization
}

// GetBearer returns the bearer string for a request to the Striketracker API
func (w *AuthWrapper) GetBearer() string {
	return ""
}

// AuthenticationResponse is the response from authenticating against the API
type

// Authentication encapsulates the user/pass structure for non-header-token auth
type Authentication struct {
	Username     string
	Password     string
	AccessToken  string
	Expiry       int
	RefreshToken string
}

// Authorization encapuslates when a user has a header token
type Authorization struct {
	AuthorizationHeaderToken string
}
*/
